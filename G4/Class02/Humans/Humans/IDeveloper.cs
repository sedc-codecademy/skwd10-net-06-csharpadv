namespace Humans
{
    /// <summary>
    /// Interface for modelling humans that can code
    /// </summary>
    internal interface IDeveloper
    {
        // Anyone who implements this interface must have a Code() method that does not return anything
        void Code();
    }
}
